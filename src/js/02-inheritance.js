/**
 * Наслідування класів
 *
 *  - extends
 *  - Конструктор дочірнього класу (super)
 *  - Методи дочірнього класу
 */

/* class Animal {
  constructor(name, age, paws, foo) {
    this.name = name || '';
    this.age = age || 0;
    this.paws = paws || 4;
  }

  sleep() {
    console.log('Zzzzz');
  }

  eat() {
    console.log('Ням ням ням');
  }

  showInfo() {}
}

class Dog extends Animal {
  constructor(name, age, paws, typeVoice) {
    super(name, age, paws);
    this.typeVoice = typeVoice;
  }
  sayHello() {
    console.log('Woof Woof');
  }
}

class Cat extends Animal {
  constructor(name, age, paws, type) {
    super(name, age, paws);
    this.type = type;
  }

  sayHello() {
    console.log('Myaw Myaw');
  }
}

class Mouse extends Animal {
  constructor(name, age, paws, type) {
    super(name, age, paws);
  }

  sayHello() {
    console.log('Pi pi pi');
  }
}

const cat = new Cat('Marsik', 4, 4, 'Pop');
const dog = new Dog('Lord', 5, 4, 'Bass');
const mouse = new Mouse('Perris', 1, 4);

console.log(cat, dog, mouse);

cat.sayHello();
dog.sayHello();

class MyDog extends Dog {} */
